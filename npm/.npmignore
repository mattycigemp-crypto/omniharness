# This file exists so npm does not fall back to the repository .gitignore,
# whose "dist/" rule would strip the compiled CLI from the published tarball.
# Nothing here is excluded — the "files" field in package.json controls what
# ships.
